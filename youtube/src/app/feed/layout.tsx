interface layProps {
    childern: React.ReactNode;
}

const layout = ({childern}:layProps) => {
    return (
        <div>
            what?
            {childern}             
        </div>
    );
};
export default layout;